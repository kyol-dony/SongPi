pyaudio
# Pin shazamio to the pure-Python build to avoid native-core segfaults on macOS.
shazamio==0.7.0
requests
pillow
screeninfo
# audioop was removed in Python 3.13+. This backport restores it so pydub works.
audioop-lts; python_version >= "3.13"
